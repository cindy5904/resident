{% extends 'base.html.twig' %}

{% block title %}Carte provisoire{% endblock %}

{% block body %}
<div class="my-5 text-center">
    <h1 class="mb-4">Justificatifs Carte Provisoire</h1>
</div>

<div class='d-flex justify-content-center'>
    <ul>
        <li>Carte grise</li>
        <li>Justificatifs de domicile de moins de 3 mois</li>
        <li>Attestation assureur si co-conducteur</li>
    </ul>
</div>
<div class="d-flex text-center my-5 m-4 gap-5 justify-content-center" style="gap: 30px;">
    <div>
        <a href="{{path('app_home')}}" class="btn btn-beige px-4 py-2 rounded" style="background: #c8ad7f ; border: none;">Retour</a>
    </div>
    <div>
       <a href="{{path('carte_provisoire.new')}}" class="btn btn-beige px-4 py-2 rounded" style="background: #c8ad7f; border: none">Ok</a>
    </div>
</div>
<div class="d-flex justify-content-center">
    <a class="rounded border btn px-4 py-3" style="background: #c8ad7f; border: none" href="{{path('carte_provisoire.index')}}">Voir la liste de toutes les cartes provisoires</a>
</div>

{% endblock %}
