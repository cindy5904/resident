{% extends 'base.html.twig' %}

{% block title %}Carte Travaux{% endblock %}

{% block body %}
<div class="my-5 text-center">
    <h1 class="mb-4">Justificatifs Carte Travaux</h1>
</div>

<div class='d-flex justify-content-center'>
    <ul>
        <li id="liste"><span style='font-size:30px; color: orange'>&#8594;</span> Carte grise de l'entreprise intervenante</li>
        <li id="liste"><span style='font-size:30px; color: orange'>&#8594;</span> Devis à l'adresse de la zone bleue</li>
    </ul>
</div>
<div class="d-flex text-center my-5 m-4 justify-content-center" id="gap">
    <div>
        <a href="{{path('app_choice')}}" class="btn px-4 py-2 rounded" id="bouton">Retour</a>
    </div>
    <div>
       <a href="{{path('carte_travaux.new')}}" class="btn px-4 py-2 rounded" id="bouton">Ok</a>
    </div>
</div>
<div class="d-flex justify-content-center">
    <a class="rounded border btn px-4 py-3"  href="{{path('carte_travaux.index')}}" id="bouton">Voir la liste de toutes les cartes travaux</a>
</div>

{% endblock %}